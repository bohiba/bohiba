import 'package:flutter/material.dart';

class AddManagerPage extends StatelessWidget {
  const AddManagerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [
          Text('Manager Detail'),
        ],
      ),
    );
  }
}
